<Window x:Class="Layouter.Plugins.PluginCompilerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Layouter.Plugins"
        mc:Ignorable="d"
        Title="插件编译" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="插件源代码文件:" Margin="0,5"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SourceFileTextBox" Grid.Column="0" Margin="0,0,5,0"/>
            <Button x:Name="BrowseSourceButton" Grid.Column="1" Content="浏览..." Width="80" Click="BrowseSourceButton_Click"/>
        </Grid>

        <TextBox x:Name="SourceCodeTextBox" Grid.Row="2" Margin="0,10" 
                 AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"/>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="输出文件:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="OutputFileTextBox" Grid.Column="1" Margin="0,0,5,0"/>
            <Button x:Name="BrowseOutputButton" Grid.Column="2" Content="浏览..." Width="80" Click="BrowseOutputButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CompileButton" Content="编译" Width="80" Margin="0,0,10,0" Click="CompileButton_Click"/>
            <Button x:Name="CloseButton" Content="关闭" Width="80" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
