<UserControl x:Class="Layouter.Plugins.MemoryMonitorPlugin.MemoryMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Layouter.Plugins.MemoryMonitorPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="总物理内存:" Margin="0,5" />
        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TotalMemoryTextBlock" Text="0 MB" Margin="10,5" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="可用物理内存:" Margin="0,5" />
        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="AvailableMemoryTextBlock" Text="0 MB" Margin="10,5" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="内存使用率:" Margin="0,5" />
        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="MemoryUsageTextBlock" Text="0%" Margin="10,5" />

        <ProgressBar Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" x:Name="MemoryUsageProgressBar" 
                     Height="20" Margin="0,10" Minimum="0" Maximum="100" Value="0" />

        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Content="释放内存" 
                Height="30" Margin="0,10" Click="ReleaseMemoryButton_Click">
            <Button.ContentTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Layouter;component/Resources/Icons/memory.png" 
                               Width="16" Height="16" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>
    </Grid>
</UserControl>
